import { Spectral, Bricolage_Grotesque } from 'next/font/google';

export const bricolage = Bricolage_Grotesque({ subsets: ['latin'] });

export const spectral = Spectral({
  weight: ['400', '700'],
  subsets: ['latin'],
});